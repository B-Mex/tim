# --- Laufzeit & Umgebung (wird lokal erzeugt) ---
venv/
logs/
memory/
__pycache__/
*.pyc
.DS_Store

# --- Grosse Binaries: Bezugsquellen stehen im README ---
modelle/
whisper-models/
piper/
wakeword/
searxng/
*.gguf
*.pt
*.onnx

# --- Private Konfiguration (Vorlagen liegen daneben als *.example) ---
config/autonomie.conf
config/ha_token.secret
config/datenschutz_muster.txt
config/com.mexla.iogpu-limit.plist

# --- Laufzeit-Schalter (liegen nur, solange etwas laeuft) ---
# Beide werden von den Pruefstaenden angelegt und im finally wieder
# geloescht. Ein eingecheckter Schalter wuerde auf einem anderen Rechner
# eine Pruefung vortaeuschen, die dort nie lief - und die Shell
# zuhalten, ohne dass jemand weiss warum.
config/PRUEFUNGSMODUS
config/PRUEFUNGSLAUF

# --- Lokale Altstaende/Sicherungen ---
# Sicherungen, die ein Patch neben der Datei ablegt. Frueher stand
# hier nur *.vor_gptoss - jede neue Sicherung musste einzeln
# nachgetragen werden, und am 01.09.2026 lagen prompt fuenf
# uneingetragene im Baum.
*.vor_*
*.vor_yoloe
*.orig
_backups/

# --- Laufzeitzustand der Kamera (legt der Dienst selbst an) ---
kamera/auge.json
kamera/anzeigen.json
kamera/messfeld.json

# --- Neue Oberflaeche: solange im Bau, nicht veroeffentlichen (24.08.2026) ---
oberflaeche/zentrale_neu.html

# --- Berichte sind Laufzeitdaten: bleiben lokal (26.08.2026) ---
berichte/

# Sperrdatei der Pruefungsflagge (pruefungsflagge.py, 03.09.2026) - Betrieb, kein Code
config/PRUEFUNGSLAUF.lock

# Lokale Anweisungen fuer Claude Code im Terminal - privat, nie ins oeffentliche Repo
CLAUDE.local.md
